fastapi==0.115.8
uvicorn[standard]==0.34.0
sqlalchemy==2.0.38
pymysql==1.1.1
cryptography==45.0.2
pandas==2.2.3
numpy==2.2.3
scikit-learn==1.6.1
bcrypt==4.2.1
python-dotenv==1.0.1
python-multipart
pillow
transformers

# PyTorch for CLIP ingredient recognition
# For CPU-only (lighter, faster install for local dev):
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# For GPU (CUDA 11.8):
#   python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# Or just use default (auto-detect):
torch
torchvision

google-auth
google-auth-oauthlib
google-auth-httplib2
requests

# Twilio SMS
twilio==9.3.7
